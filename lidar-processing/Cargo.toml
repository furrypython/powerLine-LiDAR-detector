[package]
name = "lidar-processing"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
kdtree = "0.8.1"
las = {version = "0.7.6", features = ["laz"]}
nalgebra = "0.35.0"
rand = "0.8.5"
